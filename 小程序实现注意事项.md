# 恰好俱乐部小程序实现注意事项

这份说明给第一次做小程序的人使用。当前项目已经是可以导入微信开发者工具的移动端项目，不是只能点击展示的网页原型。

当前用户链路：开场动效 -> 微信登录 -> 6 个活动偏好问题 -> 6 个关系实践问题 -> 基础信息和头像 -> 初回画像 -> 专属页 -> 发现活动 -> 活动详情与报名意向。

> **路线A（2026-08-26 起）**：为走"个人主体 + 工具-预约/报名"轻审核，需求广场相关功能（需求广场、需求卡详情、评论、共鸣、收藏、发布需求卡）已隐藏，代码用 `featureFlags.socialSquare = false` 控制；恢复时需重新开启并加回页面块。每日一问保留为"只给小CC看"的私密表达。改造清单见《路线A-隐藏需求广场改造清单.md》。

## 1. 设备和尺寸

- 设计基准按常见的 375 x 812 手机屏幕考虑，同时兼容更小的 iPhone SE 和常见安卓机。
- 页面样式使用 `rpx`：微信小程序以 750rpx 作为设计宽度，屏幕会自动缩放，不要把页面写死成某一台手机的像素。
- 底部导航已经预留 iPhone Home Indicator 的安全区域，安卓机也不会被底部导航遮住。
- 长文本、活动标题和需求标签允许换行，不能依赖固定宽度展示完整内容。
- 所有主要按钮至少保留约 80rpx 的可点击高度，避免用户点不中。

## 2. iOS 和安卓的差异

- iOS 第一次录音会弹出系统麦克风授权；安卓可能在系统设置里单独关闭麦克风，需要给用户“去设置”的入口。
- iOS 更容易出现底部安全区域，不能把按钮固定贴在屏幕最底部。
- 安卓字体渲染通常比 iOS 更紧凑，活动标题和按钮文字必须允许换行，不能只在 iPhone 上检查。
- 真机录音和开发者工具模拟器的表现不同，语音功能必须至少用一台 iPhone 和一台安卓真机测试。
- 不要用系统默认的 `px` 作为主要布局单位；小范围的边框可以使用 `1rpx`。

## 3. 当前已经实现的真实功能

- 语音回答：长按“按住说话”开始录音，松开发送，最长 60 秒。
- 录音状态：正在录音、秒数、松开发送、取消录音、录音失败提示。
- 录音回放：回答生成后可以点击“播放录音”检查声音。
- 权限处理：首次申请麦克风权限；用户拒绝后可以引导到系统设置重新打开。
- 权限兜底：如果用户没有授权麦克风，QA 页面仍然可以直接输入、编辑并提交文字，不会被权限卡住。
- 头像选择：基础资料页可以从相册或相机选择头像，头像会同步到画像页、活动主页和“我的”。
- 每日一问（私密表达）：专属页每天一问，已提交内容随本人资料同步云端，默认不公开；未提交草稿仍只保存在本机。
- 发布活动：入口已恢复，可填写内容、日期、地点、报名人数上限、价格、海报与流程；当前发布结果仍只保存在本机，正式公开前必须补主理人角色鉴权、活动审核和 `activities` 云端数据层。
- 活动报名意向：经 `activityData` 云函数写入本人 `registrations` 记录；云端以活动人数上限判断剩余名额，达到上限后按钮显示“已满员”且拒绝新增报名。网络失败时先保存本机并提示，联网后自动重试；如果重试时已经满员，则不再占用名额。
- 参与活动记录：“我的 → 参与活动”进入独立页面，默认展示过去参与清单，可切换查看报名意向；空状态可去发现活动，列表项可继续打开详情并按原路径返回。
- 初级三张 QA 卡仍然是必答，不显示跳过；中级六张卡由用户主动选择。

## 4. 关于语音转文字

当前版本已经真实保存录音，并使用演示文字展示“转写后如何确认再发送”的产品流程。真正上线时，还需要把录音文件上传到后端，再接入语音识别服务，返回真实文字。

正式上线的流程应该是：录音 -> 上传 -> 语音识别 -> 用户确认文字 -> 保存回答。未经用户确认，不要直接把识别结果写入画像。

## 5. 建议的字号层级

- 首屏品牌名：44-52rpx
- 页面主标题：32-40rpx
- 活动标题：28-32rpx
- 正文：24-28rpx
- 辅助说明：20-24rpx
- 不建议把重要说明做得小于 20rpx。

## 5.1 选项排列约定（2026-08-12 小CC确认）

- 可选项字段（性别、学历、居住地、家乡等）**统一用系统滑动选择器（picker）**，不要全展开，保持排版统一。
- 需要想象和表达的场景题（如平行宇宙）用"灵感卡 + 自由文本"，不强迫选择。
- 短选项需要展示时可横排，不纵向堆叠。
- **页面内的选项胶囊（专属页每日一问、需求发布碎片等）必须一次性全部展示**，允许自动换行（flex-wrap），禁止用横向滑动（scroll-x）把部分选项藏在屏幕外（2026-08-25 小CC强调）。

## 6. 正式产品还需要补齐的内容

- 微信登录和用户身份绑定：正式环境需要服务端保存用户资料，不能只存在页面内存里。
- 隐私保护：年龄、性别、职业、关系需求和语音都属于需要谨慎处理的个人信息，发布前要准备隐私政策和授权说明。
- 活动报名：需要服务端记录报名状态、候补、取消和活动通知。
- 活动审核：主理人发布的活动先进入待审核状态，由小CC审核后再公开。
- 消息触达：需要配置订阅消息模板和用户授权，不能默认无限推送。
- 图片上传：正式环境要压缩图片、限制大小，并把图片上传到云存储，不要长期依赖本地临时路径。
- 空状态和错误状态：没有合适活动、网络断开、报名已满、活动取消，都要有清楚的下一步操作。

## 7. 导入和体验检查

1. 用微信开发者工具导入 `qiahao-miniprogram` 文件夹。
2. AppID 可以先使用测试号或游客模式，正式发布前换成自己的小程序 AppID。
3. 在模拟器检查页面布局，再用真机预览检查录音、相册、相机、底部安全区域和字体换行。
4. 重点走一遍完整路径：欢迎页 -> 三个基础问题 -> 初级三张卡 -> 选择中级问题 -> 基础资料 -> 头像 -> 初回画像 -> 活动报名。
5. 真机首次点击语音时，允许麦克风权限；如果之前拒绝过，到手机设置重新开启后再测试。

## 8. 顶部导航与返回按钮自查清单（每次自查必跑）

1. 每个带顶栏的页面：返回箭头在最左边、标题严格居中、点击标题不会返回。
2. 返回键点击区域只在箭头附近（约 44pt 见方），远离箭头点击不触发返回。
3. 点返回只回到"上一个页面"（例：初印象画像 → 主页后，返回应回到初印象画像，不能回到欢迎页/登录页）。
4. 底部 Tab 页（活动 / 需求广场 / 我的）无返回按钮。
5. 从欢迎页进入的流程，返回一路能退回欢迎页，不会卡死。
6. 所有按钮文字必须垂直水平居中，与「告诉小CC」按钮一致；重点检查：报名意向/已满员、补充一下、筛选按钮、登录、头像编辑、分享、清除筛选和返回按钮。左对齐的问答选项保持原设计。

## 9. 「听你们的」续场推荐（2026-08-30 恢复）

- 活动详情恢复“听你们的”板块，按问答状态、关系偏好和散场时间选择 3 个续场灵感。
- 支持“换一批”和“复制这三个”；活动群聊入口与相关交互已删除。
- 不展示未经商户确认的折扣、权益和确定性营业承诺；距离与现场情况明确提醒用户出发前再次确认。

## 10. 需求卡手动发布格式（2026-08-25 小CC确认）

> 2026-08-26 路线A：该功能已隐藏，本节保留作为将来恢复时的设计依据。

- 发布器采用"一句话 + 心动碎片 + 氛围封面"，不做多步问卷，目标 30 秒内完成，降低输入成本。
- 灵感开头（点选填入输入框）：想遇见 / 最近在找 / 周末想 / 有点希望。
- 心动碎片两排（可点选、可不选）：场景（散步/深夜/周末/晚霞/雨天/咖啡）+ 关系（认真/轻松/同频/慢慢来/被看见/这周能约）。
- 氛围封面（可选用现有海报图）：散步 / 深夜 / 晚餐 / 午间 / 猫。
- 发布后：需求卡标题=用户句子，subtitle=前两个场景碎片（如"散步 · 晚霞"），tags=所选碎片，封面=选中氛围图；未选封面用默认午间图。
- 画像识别（无感）：碎片和句子关键词后续用于需求类型 / 活动偏好 / 认识节奏 / 可约档期聚类，需求广场按标签聚合，共鸣人数达标触发小CC活动提案。
- 与专属页"你的声音"互补：专属页问答自动转需求卡发布；需求 Tab 发布器是主动、自由表达入口。
- 进阶项（下一阶段）："让恰好帮你表达"——AI 润色句子，需后端支持。

## 11. 反馈回流画像与联动（2026-08-25 新增）

- 行为标签 `behaviorTags`：共鸣 / 收藏 / 发布需求卡（2026-08-26 路线A 起已暂停，恢复广场时启用）；活动"考虑一下"的原因继续回流。专属页头像下方标签实时更新（最多 8 个）。
- 活动"不喜欢这类"：记入 `dislikedActivityIds`，专属页推荐卡和后续推荐排除该活动，不会再次出现。
- 持久化：`behaviorTags`、`dislikedActivityIds`、`considerReasons`、每日一问私密表达（`myNeed / myNeedQuestion / needAsked`）随 `persistDraft` 保存/恢复。

## 12. 专属页与需求页功能测试清单（每次自查必跑）

### 专属页（home）
1. 已登录用户进入后直达专属页；未登录走欢迎页。
2. 顶部标题/头像/昵称正常；点猫有"喵～"提示。
3. 画像区展示摘要和标签，不显示没有真实计算依据的“完善度百分比”。
4. 活动海报轮播自动切换，点击海报打开对应活动详情；详情返回回到专属页。
5. 每日一问：选项一次性全部展示、不滑动；问题每天轮换（7 题）。
6. 回答流程：选选项/填其他 → 告诉小CC → 出现"你的声音"卡（私密表达，不再公开发布）。
7. "补充一下"：重新进入提问，且旧选项已清空；重新提交后更新原卡而非新增。
8. 回答后重启小程序：needAsked 状态保持。

### 全局（路线A 3 tab）
9. 底部 tab 只有 3 个（专属 / 发现 / 我的），无"需求"入口。
10. 任何页面都没有入口能打开需求广场、需求卡详情、发布器、评论、收藏。
11. 返回键统一：贴最左、标题居中、不误触。
12. 老用户本地缓存（带旧需求字段）进入不报错，旧字段自动忽略。

### 发布活动与活动记录
13. “我的”显示发布活动入口；标题、日期、时间、地点和报名人数上限缺失时不得生成活动。
14. 报名人数上限为 1–200 的整数；达到上限时活动详情显示“已满员”，已报名用户仍可取消并释放名额。
15. 标签最多 4 个、流程最多 6 步；当前发布结果只保存在本机，不得提示为已公开发布。
16. 点击“参与活动”进入独立页面，默认过去参与，可切换报名意向；顶部返回、列表详情、详情返回链路正确。
17. 活动详情没有“活动群聊”入口；“听你们的”换一批与复制操作可用，长文本正常换行。

> 需求广场相关测试项（排序、共鸣、收藏、评论、发布、详情、活动回应）在路线A下已隐藏，恢复广场时按《路线A-隐藏需求广场改造清单.md》重新启用后再测。

### 已知限制（2026-08-26 已修复两项）
- 上传的封面/头像已通过 `saveFile` 保存到本地持久目录，重启小程序不失效；正式版仍需云存储做跨设备同步。
- 旧字符串评论已在打开详情时统一转成对象（带作者/首字母/内容），不再有 wx:key 警告。

## 13. 数据库接入准备（2026-08-26）

- 数据模型、字段映射、索引、接入步骤见《数据库设计.md》。
- 云函数规划见 `cloudfunctions/README.md`。
- 接入顺序：开通云开发 → 建集合 → 云函数 → `wx.cloud.init` → 页面读写改库（保留本地缓存兜底）→ 上传文件走云存储。

## 14. 真实微信登录（2026-08-26 云开发已开通，登录函数已部署）

- 云环境 ID：`cloud1-d5gw01teoed1301d0`（已写入 `config.js` 的 `cloudEnv`）。
- 登录云函数：`cloudfunctions/login` 已部署到该环境；客户端先调用 `wx.login` 确认可用会话，云函数只用 `cloud.getWXContext()` 取得 openid，不信任客户端身份参数。
- 客户端已接入：点“微信登录”→ `wx.login` → 云函数 `login` → 取得真实 openid，再按本人实际完成进度继续流程。云登录失败时停留在登录页并明确提示，不创建演示身份。
- 环境配置：`config.js` 的 `cloudEnv` 已填环境 ID；如需换环境改这里即可。
- 头像昵称：基础资料页已改为"选择微信头像"（`open-type="chooseAvatar"`）+ 昵称输入框（`type="nickname"`），样式与原来一致。
- 重新部署方法：微信开发者工具左侧「云函数」→ 右键 `login` → 上传并部署：云端安装依赖。
- 真机验证：重新生成预览二维码 → 手机扫码 → 点“微信登录” → 新用户应从第一组问答开始，已完成资料的老用户应从云端恢复到最新进度；之后可在云开发控制台「数据库 → users」看到本人唯一记录。

## 15. 用户资料云端化（2026-08-29）

- 验收总基线见《ACCEPTANCE_CRITERIA.md》，P0 未通过不得发布。
- 新增 `cloudfunctions/userProfile`：`get` / `save` 只读写当前 `cloud.getWXContext()` 对应用户；请求中出现 `openid`、`_openid`、`userId`、`targetOpenid` 等身份字段会直接拒绝。
- `login` 与 `userProfile` 使用相同的确定性 users 文档 ID；首次调用会迁移本人旧随机文档，并清理重复记录。
- 云端资料放在 `users.profile`，覆盖基础资料、入口/关系/QA 问答、画像、行为标签与每日来信的已提交内容；`visibility` 固定为 `private`。
- `qiahaoDraft` 继续作为本地缓存。页面先用本地数据快速恢复，再读取云端；正常情况云端优先，云端没有的字段由本地补齐；本地存在待同步修改时，本地版本优先并自动重试上传。
- 旧版没有 `_profile*` 同步元数据的缓存会自动兼容：云端没有资料时上传旧本地资料，云端已有资料时先合并，不删除活动等仅本地字段。
- 同步失败会保留 `_profileSyncPending: true`，并提示“内容已保留在本机”；不得因失败清空表单或覆盖本地草稿。
- 本地 `wxfile://` 头像路径不能跨设备，当前不会写入云端；头像需先上传云存储成为 `cloud://` 或可信 `https://` 地址后再同步。
- 部署顺序：先上传部署 `cloudfunctions/login`，再上传部署 `cloudfunctions/userProfile`（均选择“云端安装依赖”），最后重新编译小程序。
- `users` 集合权限设为“仅管理端可读写”（`read: false, write: false`），避免客户端绕过云函数直接查询资料；云函数权限将 `login` / `userProfile` 设为仅已登录且非匿名用户可调用。
- 部署后在云开发控制台确认：同一测试账号只有一条 `users` 记录、`visibility=private`、`profile` 有基础资料/问答/画像、更新时间随修改变化。
- 自动检查：根目录运行 `npm run lint` 与 `npm test`。交互与跨端恢复仍必须按验收基线在 iPhone + Android 真机验证。
