# 恰好俱乐部 · 数据库设计（微信云开发）

> 目标：当前数据存在手机本地（`persistDraft`），换设备会丢。接下来接入微信云开发（CloudBase），让用户数据真正存下来。本文件是接入前的设计文档，供开发时对照。

## 一、数据库选型

- 使用微信云开发（CloudBase）免费环境：云数据库（文档型，类似 MongoDB）+ 云函数 + 云存储。
- 每条文档由微信自动带 `_openid`（用户身份），天然区分用户数据。
- 正式版才需要；开发调试可用"测试环境"（不花钱）。

## 二、集合（Collections）设计

### 1. `users`（用户档案）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `_openid` | string | 微信自动，用户唯一标识 |
| `nickname` | string | 昵称 |
| `avatar` | string | 头像（云存储 fileID） |
| `gender` / `birth` / `height` / `education` | string | 基础资料 |
| `hometown` / `area` / `occupation` | string | 家乡 / 居住地 / 职业 |
| `scene` | string | 平行宇宙场景回答 |
| `profileTitle` | string | 画像标题（如"慢热观察 · 边界清晰"） |
| `profileDims` | array | 画像维度（认识节奏/表达方式/安全感与边界/活动偏好） |
| `behaviorTags` | array | 行为反馈标签（共鸣/收藏/发布/考虑原因回流） |
| `homeQuestionId` | string | 当天已答的每日一问 id（防重复） |
| `createdAt` / `updatedAt` | date | 创建/更新时间 |

### 2. `needs`（需求卡）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `_openid` | string | 发布者 |
| `title` | string | 标题（≤14 字） |
| `content` | string | 内容（≤200 字） |
| `question` | string | 小CC提问（回答模式才有） |
| `topic` / `topicCat` | string | 话题文字 / 话题分类 |
| `tags` | array | 标签（#开头） |
| `cover` | string | 封面（云存储 fileID 或系统图） |
| `resonanceCount` | number | 共鸣数 |
| `commentCount` | number | 评论数 |
| `status` | string | 状态：open（招募中）/ matched（已成局） |
| `similar` | bool | 是否和你相似 |
| `createdAt` | date | 发布时间 |

### 3. `comments`（评论）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `needId` | string | 所属需求卡 |
| `_openid` | string | 评论者 |
| `author` | string | 昵称 |
| `avatar` | string | 头像 |
| `text` | string | 内容 |
| `createdAt` | date | 时间 |

### 4. `need_actions`（需求互动：共鸣 / 收藏）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `needId` | string | 需求卡 id |
| `_openid` | string | 用户 |
| `type` | string | `resonance` 或 `save` |
| `createdAt` | date | 时间 |

> 唯一约束：同 `needId + _openid + type` 只允许一条（去重）。

### 5. `activities`（活动）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `_id` | string | 活动 id（dinner/ai/walk/workshop/lunch） |
| `title` / `subtitle` | string | 标题 / 副标题 |
| `date` / `weekday` / `time` | string | 日期 / 星期 / 时间 |
| `location` / `people` / `fee` | string | 地点 / 人数 / 费用 |
| `poster` | string | 海报（云存储 fileID） |
| `tags` | array | 标签 |
| `crowd` | string | 适合谁 |
| `schedule` | array | 活动流程（time+title） |
| `status` | string | 招募中 / 报名中 / 已结束 |
| `createdAt` | date | 创建时间 |

### 6. `registrations`（报名）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `activityId` | string | 活动 |
| `_openid` | string | 用户 |
| `status` | string | `pending`（待支付）/ `confirmed`（已报名）/ `cancelled`（已取消） |
| `reason` | string | 取消/考虑原因 |
| `createdAt` / `updatedAt` | date | 时间 |

## 三、本地数据 → 数据库字段映射

现在 `persistDraft` 存的 `qiahaoDraft`，接入后按此映射：

| 本地字段 | 目标集合 |
| --- | --- |
| `basicInfo` / `avatarUrl` / `profileDims` / `profileTitle` / `behaviorTags` | `users` |
| `communityNeeds` | `needs`（发布时写库；读取时合并系统卡） |
| `activeDemand.comments` | `comments` |
| `resonatedNeedIds` / `savedNeedIds` | `need_actions` |
| `activityFeed`（内置） | `activities`（小CC后台维护） |
| `registeredActivities` / `demandHistory` | `registrations`（历史可由库反查） |
| 每日一问 `homeQuestion` | `home_questions` 集合（小CC维护题库） |

## 四、索引建议

- `needs`：`createdAt` 降序（最新）、`resonanceCount` 降序（最共鸣）、`status`
- `comments`：`needId + createdAt`
- `need_actions`：`needId + type`、`_openid + type`
- `registrations`：`activityId + status`、`_openid`

## 五、接入步骤（按顺序）

1. 微信开发者工具 → 云开发 → 开通（免费额度）。
2. 项目根目录创建 `cloudfunctions/`，新建云函数：`login`（登录返回用户）、`need`（需求卡增删查）、`comment`（评论）、`action`（共鸣/收藏）、`register`（报名）。
3. `app.js` 初始化：`wx.cloud.init({ env: "你的环境ID" })`。
4. 页面读写从本地 `persistDraft` 改为调用云函数/云数据库（保留本地缓存做离线兜底）。
5. 上传海报/头像/封面的临时文件改为先上传云存储拿 fileID，再存库。
6. 隐私保护指引：配置麦克风、相册（写入）、摄像头权限后，真机相册上传才能用。

## 六、注意

- 演示数据（6 张系统需求卡、5 场活动、评论池）保留在客户端作为兜底/离线展示，正式版以库为准。
- 金额、报名状态等涉及交易的数据，接入后必须走服务端校验，不能只信客户端。
- 所有集合读写加"仅创建者可见/可写"权限规则，避免用户互相改数据。
