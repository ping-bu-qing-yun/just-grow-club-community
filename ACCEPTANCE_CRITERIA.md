# 恰好俱乐部小程序验收基线

> 生效日期：2026-08-29  
> 适用范围：微信原生小程序源码、云函数、云数据库和真机体验。  
> 规则：P0 未全部通过不得发布；P1 应进入当前或紧邻迭代，并在未通过时留下明确记录。

## P0：发布阻断项

### 1. 核心链路

- 欢迎页 → 登录 → 基础问题 → 关系问答 → 基础资料 → 初回画像 → 专属页 → 活动浏览/详情全程无阻断。
- 云端不可用时，用户仍可继续填写；已经输入的内容不得因请求失败、重开小程序或切换页面而丢失。
- 登录、资料保存/恢复等关键异步操作必须有 loading、成功或失败反馈，不允许静默卡住。

### 2. 身份、数据与隐私

- 云函数中的用户身份只允许来自 `cloud.getWXContext()`；客户端传入的 `openid`、`_openid`、`userId`、`targetOpenid` 等身份字段必须拒绝或忽略，不能据此读写数据。
- 同一微信用户在 `users` 集合中最终只能保留一条记录；重复旧记录在安全迁移后清理。
- 基础资料、问答和画像写入云端；`qiahaoDraft` 保留为本地缓存和离线兜底。
- 启动后先显示本地缓存，再完成云端优先恢复和字段合并；旧版无同步元数据的本地缓存可迁移。
- 云端同步失败时保留本地数据，并用用户能理解的文案明确提示；恢复网络后可再次同步。
- 年龄/生日、性别、地区、职业、关系问答、画像、语音等私密数据默认不公开。当前阶段只允许本人通过受身份校验的云函数读写。
- 日志不得输出完整 `openid`、资料正文、手机号、验证码、语音内容或其他私密数据。

### 3. 兼容性与 UI

- 每次涉及交互或云端数据的发布候选版，至少在一台 iPhone 和一台 Android 真机完整回归。
- 主要按钮可点击高度不小于 `80rpx`。
- 底部固定区域兼容 iPhone 安全区；安卓常见屏幕不被遮挡。
- 活动标题、按钮文案、标签和资料长文本可换行，不依赖写死宽度展示完整内容。

## P1：质量改进项

- `lint`、`test` 命令可执行，并逐步纳入 CI；新增纯业务逻辑优先带单元测试。
- 业务逻辑逐步从 `pages/index/index.js` 抽离到可复用、可测试模块，避免继续扩大单页职责。
- 无数据、弱网、断网、超时、权限拒绝和服务异常均有明确空状态或错误状态，并提供下一步操作。
- 控制 `setData` 的字段规模和调用频率；避免整棵大对象、长列表和不可见区域反复更新。
- 页面层级、按钮、表单、反馈、留白和文案保持 WeUI/微信原生体验方向一致；不要求一次性引入组件库。
- 工程逐步采用 ESLint、Prettier、Stylelint、测试与 CI；工具升级不能阻断本期核心体验。

## 第一阶段专项验收：用户资料云端化

1. **唯一用户记录**：同一账号反复登录、重开、并发触发登录/资料保存后，`users` 最终只有一条本人记录。
2. **资料写云端**：基础资料、入口问答、关系问答、QA 回答和生成画像可在 `users.profile` 中读取。
3. **重开恢复与合并**：重开后从云端恢复；云端缺少的旧字段可由本地缓存补齐，不破坏活动等仅本地数据。
4. **最新修改可见**：修改资料或画像并成功同步后，退出重进显示最新版本。
5. **不能指定他人身份**：调用资料云函数时传入任意 `openid`/`_openid`/`userId`/`targetOpenid` 均被拒绝；正常请求不携带这些字段。
6. **失败不丢数据**：断网、云函数未部署或数据库异常时，本地 `qiahaoDraft` 仍是最新内容，并出现“已保留在本机/稍后重试”类提示。
7. **主流程无回归**：登录、问答、画像生成、专属页和活动浏览按原 UI 与导航继续可用。

### 专项通过方式

- 自动检查：`npm run lint`、`npm test`。
- 开发者工具：分别验证首次用户、旧本地缓存用户、已有云端资料用户、云端失败四种场景。
- 云开发控制台：核对唯一 `users` 文档、`visibility: "private"`、资料字段和更新时间。
- 云开发控制台：将 `users` 集合设为“仅管理端可读写”（安全规则 `read: false, write: false`），客户端只能经本人云函数访问；`login` / `userProfile` 调用权限只允许已登录且非匿名用户。
- 真机：iPhone + Android 各跑一次专项验收 2、3、4、6、7；结果记录在发布/验收报告中。

## 外部基线与采用理由

- [微信小程序官方示例 `miniprogram-demo`](https://github.com/wechat-miniprogram/miniprogram-demo)：贡献说明明确要求通过 `npm run lint`，仓库也包含测试与工作流目录，因此将可执行 lint 设为 P1 基线。
- [微信小程序自定义组件测试工具 `miniprogram-simulate`](https://github.com/wechat-miniprogram/miniprogram-simulate)：提供在 Node/JSDOM 或浏览器环境中渲染组件树的测试方案；当前项目先覆盖纯同步/身份策略逻辑，组件拆分后再接入模拟测试。
- [WeUI MiniProgram](https://github.com/wechat-miniprogram/weui-miniprogram) 与 [WeUI WXSS](https://github.com/Tencent/weui-wxss)：由微信官方团队维护，定位为微信原生视觉和交互体验的基础组件/样式参考，因此将微信原生一致性设为 P1 UI 基线。
- [TDesign MiniProgram](https://github.com/Tencent/tdesign-miniprogram) 与 [零售小程序模板](https://github.com/Tencent/tdesign-miniprogram-starter-retail)：成熟微信小程序工程采用 ESLint、Prettier、Stylelint、测试和工作流，作为本项目逐步补齐工程质量的参考，而不是本期强制一次性引入全部依赖。
- [CloudBase 数据库安全规则](https://docs.cloudbase.net/rule/rule-example) 与 [云函数安全规则](https://docs.cloudbase.net/cloud-function/security-rules)：私密集合使用仅管理端读写，资料函数只允许已登录且非匿名用户调用；云函数内部仍必须再次从上下文校验身份。

## 变更规则

- 新功能在开发前补充专项验收项；影响 P0 时必须先更新本文再改代码。
- 无法自动验证的真机、权限、审核和云端部署项必须明确标注“待人工验证”，不得用模拟器结果代替。
- 本文与《小程序实现注意事项.md》冲突时，安全和隐私取更严格者；产品交互约定以最新确认记录为准。
