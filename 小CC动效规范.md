# 小CC动效规范（v0.1）

> 依据已安装的 motion-design（LottieFiles）与 interface-details（detail.design）两个技能，结合小CC"温暖、低压、不催促、不花哨"的风格整理。做任何动效前先读这份。

## 一、小CC的动效性格：温暖灵动，克制有度

一句话：**动效是用来"回应"用户的，不是用来"表演"的。**

三条铁律（来自 interface-details 的决策门槛）：

1. **有用才动**：每次动效都要回答一个问题——用户因为这个动效知道了什么（按下了、成功了、出错了、内容在加载）。只为好看而动的，删掉。
2. **越常用的操作越安静**：报名按钮、返回、翻页这些高频动作，动效要快而轻；首次见面、脱单倒计时这类"值得纪念"的时刻，才允许多一点灵动的惊喜。
3. **同一动作永远一样**：同一个交互在哪个页面都必须是同样的动效，不统一比没有更糟。

## 二、小CC的动效参数（固定值，别乱改）

### 时长（三个档位）

| 用途 | 时长 | 示例 |
|---|---|---|
| 快（微反馈） | 80–180ms | 按钮按下、开关、选中态 |
| 标准（卡片/弹层） | 200–350ms | 卡片入场、弹窗、报名成功 |
| 慢（页面切换/氛围） | 400–600ms | 页面转场、脱单倒计时呼吸 |

规则：**入场比离场慢**（入场 100%，离场 65–75%）；移动距离越大，时长按比例加长（100rpx 为基准，200rpx ×1.3）。

### 缓动（动效的"语气"）

- 入场：`ease-out`（快起慢落，温柔落地）
- 离场：`ease-in`（慢起快走，干脆离开）
- 常驻/氛围：`ease-in-out`
- 旋转/进度条：`linear`
- 轻快的小弹跳：`cubic-bezier(0.34, 1.3, 0.64, 1)`（只在彩蛋和成功时刻用，幅度 ≤10%）

**禁止**：位移类动效用 linear（会显得机械、廉价）。

### 常用动效清单

| 场景 | 怎么做 | 时长 |
|---|---|---|
| 按钮按下 | 缩小到 0.96，放手回弹 | 按下 120ms / 回弹 180ms |
| 卡片入场 | 从下方 20rpx 浮起 + 淡入，列表逐张错开 40–80ms | 250ms，总错开 <400ms |
| 弹窗 | 中心放大 + 淡入；关闭时缩回触发点 | 300ms |
| 报名成功 | 对勾/小CC点头缩放弹出，轻微过冲 5–10% | 300–400ms |
| 操作失败 | 横向轻摇 2–3 次，幅度递减，不弹跳 | 300ms |
| 加载中 | 小CC元素轻呼吸（透明度 0.6↔1）或轻骨架屏 | 呼吸 2–3s 一圈 |
| 选中切换 | 高亮块在选项间滑动，不跳变 | 200ms |
| 不可点按钮被点 | 轻轻摇一下表示"听到了，还不行" | 200ms |
| 脱单倒计时彩蛋 | 数字轻微闪烁/呼吸，不自动循环播放 | 呼吸 3–4s 一圈 |

## 三、小程序的动效技术约定

1. **只动 transform 和 opacity**：优先 CSS transition/keyframes，动画走 GPU，不卡顿。禁止动画 width、height、margin、border-radius、box-shadow（安卓上特别卡）。
2. **一次只动 1–2 个属性**：一个元素最多两个属性在动，超过就删。
3. **同时最多 1/3 的元素在动**：入场用错开（stagger）排队，总错开时间不超过 400ms。
4. **同屏动画元素 <20 个**；页面隐藏时停掉循环动画。
5. **iOS 闪屏防护**：用到 3D 或大位移时加 `-webkit-backface-visibility: hidden`；安卓避免大面积 box-shadow。
6. **动效里不放关键信息**：成功、失败一定要有文字/颜色配合，不能只靠动画表达。
7. **尊重"减弱动态"的用户**：系统开启减弱动态效果时，动画退化为纯淡入淡出或直接不播（如微信 `wx.getAppBaseInfo` 可查设置，复杂动效给降级方案）。

## 四、每页动效自查（改完动效过一遍）

- [ ] 每个动效都能回答"用户因此知道了什么"？
- [ ] 高频操作是否足够快、足够安静？
- [ ] 同一个交互在别处是否一致？
- [ ] 入场 ease-out、离场 ease-in、位移没有用 linear？
- [ ] 只用了 transform/opacity？
- [ ] 总错开时间 <400ms，同时动的元素 ≤1/3？
- [ ] 真机（iPhone + 安卓）各跑一遍，不卡、不闪、不跳？

## 五、参考来源

- 已安装技能：`motion-design`、`interface-details`（下次对话起自动可用）
- 微信官方小程序设计指南：https://developers.weixin.qq.com/miniprogram/design/
- 微交互灵感库：https://detail.design
- 现成动效素材（可下载 Lottie 动画导入小程序）：https://lottiefiles.com
