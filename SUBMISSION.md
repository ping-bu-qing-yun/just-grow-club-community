# 赛方源码提交说明

## 作品定位

恰好关系俱乐部是一个帮主理人把“用户交友需求”反向生成线下活动、并按需招募到对的人的 AI Agent Demo。

## 运行说明

本源码包是静态前端 Agent Demo，可直接运行：

1. 解压 ZIP。
2. 直接打开 `index.html`，或用任意静态文件服务托管当前目录。
3. 在线版本可访问：https://ping-bu-qing-yun.github.io/just-grow-club-community/?v=a9ae795

## 依赖说明

- 不需要 MCP。
- 不需要数据库。
- 不需要后端服务。
- 不需要安装 `node_modules`。
- 不需要配置 `.env`。

当前 Demo 的演示数据、用户画像、活动提案、运营工作台、报名/预约/捡漏/取消等交互均内置在前端源码中。运行时用户操作状态保存在浏览器本地存储中，因此源码包中不包含本地调试产生的 `data/qiahao.sqlite`、`.env`、`node_modules` 等文件。

## 主要入口

- `index.html`：推荐评审入口。
- `demo.html`：备用入口。
- `assets/`：前端脚本、样式和图片资源。
- `dist/`：同步保留的静态构建产物。
- `README.md`：作品介绍、创作链路和功能说明。

## 评审建议

建议使用手机尺寸或浏览器移动端模式打开，以体验完整小程序式交互；电脑端也已做居中手机框适配。
