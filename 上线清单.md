# 恰好俱乐部小程序 · 上线清单（以"真的要上线"为标准）

- 日期：2026-08-26
- 结论：当前版本是完整的内测 Demo；离上线还差"平台合规 + 真实数据 + 支付方案 + 小CC工作台"四大块。下面按"硬门槛 → 功能对表 → 谁做什么 → 先后顺序"列出。

## 一、上线硬门槛（不满足 = 审核必拒或无法发布）

| # | 门槛 | 当前状态 | 要做什么 |
| --- | --- | --- | --- |
| 1 | 主体与类目：审核最轻的是「工具-预约/报名」（个人主体开放、无需资质）；但微信审核明确：只要小程序有用户自行发布 / 评论 / 交流（公开 UGC 广场），就属社交范畴，个人主体未开放，会被打回 | 个人主体 | 两条路线二选一：① 个人主体 + 工具-预约/报名 → 需求广场改成非公开（问卷 / 表单式，需求只发给小CC，不做公开广场和评论）；② 办个体工商户（几百元、几天）→ 保留公开需求广场，选社交类目，也为将来支付留路 |
| 2 | ICP 备案：2023 年 9 月后新小程序必须完成备案才能发布 | 未做 | 在公众平台"设置 → 基本设置 → 备案"提交，一般 1-2 周，越早越好 |
| 3 | 用户隐私保护指引 + 隐私政策 | 未配置 | 公众平台"设置 → 用户隐私保护指引"逐项勾选（头像、昵称、生日、职业、照片、位置等），并在小程序里加隐私说明；之前因认证中没填的，现在补 |
| 4 | 真实微信登录 | 模拟登录（固定"小Z"） | 接 `wx.login` + 云函数换 openid，头像昵称走微信授权（个人主体用"头像昵称填写"能力；手机号快捷验证需企业主体） |
| 5 | 内容安全（UGC 必查） | 未接入 | 需求卡、评论发布前调用微信内容安全接口（文本 + 图片），违规直接拦截——这是审核重点 |
| 6 | 数据真实落库 | 全部存手机本地，换设备就丢 | 开通微信云开发，按《数据库设计.md》建集合，写 6 个云函数（login / need / comment / action / register / activity），页面读写改库（本地缓存兜底） |
| 7 | 支付合规 | 暂不做（小CC 2026-08-26 确认） | 保持现状：报名后小CC人工确认收款；将来要收费再办商户号（个人主体办不了） |
| 8 | 报名状态真实化 | 演示状态 | 建 `registrations` 集合：报名 / 候补 / 满员 / 取消 / 已结束，人数校验真实生效 |

## 二、功能对表（每个功能：现在是什么 → 上线前要改成什么）

| 功能 | 现在 | 上线前要改成 |
| --- | --- | --- |
| 登录 | 微信快捷进入（无真实身份） | 微信登录绑定 openid，用户档案存 `users` |
| 基础资料 / 头像 | 本地保存 | 资料存库；头像上传云存储（先压缩） |
| 关系画像 | 本地计算 | 存 `users` 字段，行为反馈回流入库，持续更新 |
| 每日一问 | 本地轮换 | 记录"今天已答"，防重复，答案入库 |
| 专属页推荐 | 本地规则 | 按画像 + 活动状态真实推荐 |
| 活动详情 / 报名 | 演示报名 | 真实报名 / 取消 / 满员 / 候补，小CC可查 |
| 需求卡 | 本地发布 | 发布入库 + 内容安全 + 封面走云存储或系统图 |
| 评论 / 共鸣 / 收藏 | 本地 | 入库 + 去重 + 计数 + 内容安全 |
| 小CC工作台 | 只在网页 demo | 暂不做（小CC 2026-08-26 确认），后续需要时再加 |
| 消息通知 | 无 | （可选）订阅消息：报名成功、活动提醒 |
| 错误 / 空 / 网络态 | 部分 | 加载中、网络失败、无权限、活动已满都要有可读提示 |

## 三、平台侧手工事项（需要小CC去后台操作，代码做不了）

1. 确认主体：个人 vs 个体工商户 / 企业（决定类目和支付，最重要）
2. 在公众平台选择服务类目
3. 提交 ICP 备案
4. 配置用户隐私保护指引（逐项勾选 + 写清用途）
5. 开通微信云开发环境（创建正式环境，拿到环境 ID）
6. 如收费：申请微信支付商户号
7. 如做通知：申请订阅消息模板

## 四、技术侧事项（交给 Codex，按顺序做）

1. 开通云开发后：`wx.cloud.init` 配置（正式环境 ID）
2. 写云函数：login / need / comment / action / register / activity
3. 页面数据层改造：读库写库 + 本地缓存兜底
4. 头像 / 封面上传云存储 + 压缩
5. 内容安全接入（文本 + 图片）
6. 报名状态流转 + 工作台数据聚合
7. 支付（等商户号就绪）
8. 体验版回归 + 提审材料 + 发布

## 五、测试与发布流程

- 体验版：测试账号跑完整链路（注册 → 画像 → 报名 → 发需求 → 评论 → 共鸣）
- 真机：iPhone + 安卓各一台
- 提审备注：写清入口、功能用途、收集了哪些个人信息
- 发布：先灰度给内测群（约 20 人）验证，再全量发布

## 六、如果只做三件事（先后顺序）

1. 定路线：个人主体 + 需求广场改非公开；或办个体工商户保留公开广场（第 0 步，决定后面怎么做）
2. 微信登录 + 数据落库（内测版立得住）
3. 隐私配置 + 内容安全（审核必查）

## 七、个体工商户办理要点（上海，2026-08-26 调研）

> 路线已定：**路线A（个人主体 + 工具-预约/报名，先隐藏需求广场）**，2026-08-26 小CC确认；改造清单见《路线A-隐藏需求广场改造清单.md》。

### 需要准备的资料
- 经营者本人身份证（拍照 / 扫描）
- 经营场所证明：自有房产（房产证）或租赁合同；住宅改经营需按所在区要求（物业 / 居委会同意）
- 个体户名称（网上自主申报，如"上海市杨浦区恰好文化创意工作室"）
- 经营范围（见下方建议文案）
- 本人手机号（接收验证码）、实名认证 + 电子签名

### 办理流程（全程线上为主）
1. 打开"随申办市民云"（App / 微信小程序 / 支付宝小程序）或"上海一网通办"PC 端，搜索"个体工商户设立登记"或"个人创业一件事"
2. 实名认证 → 名称查重 → 填经营范围 → 填经营场所 → 上传身份证和场所材料 → 电子签名 → 提交
3. 审核通过（一般 0.5-1 个工作日）→ 下载电子营业执照（微信 / 支付宝"电子营业执照"小程序），纸质执照可邮寄或线下领取（免费）
4. 刻章（公章 / 财务章 / 发票章，约 100-500 元，建议刻）
5. 税务：执照办好后自动税务登记；开票再申领发票；个体户可申请定期定额，月销售额 10 万以下免征增值税（现行政策到 2027 年底，以税务局为准）
6. 银行：前期可不开对公账户，办微信支付商户号时按微信要求提供经营者银行卡
7. 年报：每年 1-6 月在"国家企业信用信息公示系统"提交年报

### 费用与时间
- 营业执照：免费；刻章约几百元；小程序微信认证：企业 / 个体户 300 元 / 年
- 时间：线上提交后一般 1 个工作日内办结，从准备到拿照约 3-7 天

### 经营范围建议文案（可直接抄）
组织文化艺术交流活动；体验式拓展活动及策划；会议及展览服务；信息咨询服务（不含许可类信息咨询服务）；咨询策划服务；市场营销策划；广告设计、制作、代理。（不要写"婚姻介绍服务"等许可类词）

### 办完执照后接小程序（重要衔接）
- 现在的 AppID 是个人主体注册的；拿到执照后，需在微信公众平台"设置 → 主体信息"发起主体变更 / 迁移（个人 → 企业 / 个体户），规则以平台为准：同一经营者有时可免公证，否则需双方确认 + 公证
- 备选：若迁移不支持或太麻烦，直接新注册一个个体户主体的小程序（AppID 会换，认证和备案反正都要做）
- 备案主体必须和执照一致，所以先办执照、再定主体、再备案

### 没有实体经营场所怎么办（线上经营，2026-08-26 官方口径）
- 上海市场监管局的官方答复（2026-07-17）：仅凭"小程序线上经营、没有线下门店"，**不能用"网络经营场所"登记**（那条通道是给淘宝 / 拼多多等平台内经营者的，需要平台出具证明，微信小程序拿不到）；也不能保证"住所承诺制"可用（只有部分区试行，需所在区市场监管所确认）
- 可行方案（按推荐顺序）：
  1. **园区集中登记地 / 集群注册**：找创业园区、孵化器挂靠注册地址，一般几百到一两千元 / 年，正规合法；杨浦创智天地附近就有这类园区
  2. **住宅地址（住改商）**：自己名下的房产，去所在街道市场监管所问本区是否允许，通常需要物业 / 居委会同意、不扰民承诺
  3. **先不办执照**：先用个人主体 + 工具-预约/报名类目上线轻量版（需求广场改非公开），等真正需要时再解决地址问题
- 最直接的确认方式：打所在区市场监管局电话或去市场监管所窗口问一句"个体工商户登记地址本区能用哪些"，10 分钟得到准确答案
