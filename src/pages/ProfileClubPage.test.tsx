import { render,screen } from '@testing-library/react';import { expect,it,vi } from 'vitest';import { ClubProvider } from '../club/ClubContext';import { QiahaoProvider } from '../state/QiahaoContext';import { ProfilePage } from './ProfilePage';
it('shows portrait growth and complete record entries',()=>{render(<QiahaoProvider><ClubProvider><ProfilePage onNotice={vi.fn()}/></ClubProvider></QiahaoProvider>);expect(screen.getByText('画像成长')).toBeInTheDocument();expect(screen.getAllByText('需求收藏')).toHaveLength(2);expect(screen.getByText('参加过活动')).toBeInTheDocument();expect(screen.getByText('我的动态')).toBeInTheDocument();expect(screen.getByText('我的消息')).toBeInTheDocument();});
