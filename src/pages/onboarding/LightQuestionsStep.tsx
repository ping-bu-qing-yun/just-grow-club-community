import { lightQuestions } from '../../club/seed';
import { useClub } from '../../club/ClubContext';
export function LightQuestionsStep({onNext}:{onNext:()=>void}){const{state,toggleLightAnswer}=useClub();const valid=state.lightAnswers.every(items=>items.length);return <section className="onboarding-body"><div className="onboarding-intro"><span>三问入门 · 约30秒</span><h1>你的回答就是最好的自我介绍</h1><p>先用三个轻问题，让我们知道你最近真正想要什么。</p></div>{lightQuestions.map((question,index)=><div className="question-section" key={question.title}><div className="question-title"><b>{index+1}</b><h2>{question.title}</h2></div><div className="answer-grid">{question.options.map(option=><button className={state.lightAnswers[index].includes(option)?'is-active':''} onClick={()=>toggleLightAnswer(index,option)} key={option}>{option}</button>)}</div></div>)}<button className="primary-button primary-button--wide" disabled={!valid} onClick={onNext}>下一步</button></section>}
