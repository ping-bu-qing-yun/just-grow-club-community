import { useState } from 'react';
import { Mic,SkipForward } from 'lucide-react';
import { qaSets } from '../../club/seed';
import { useClub } from '../../club/ClubContext';
export function QaCardsStep({onNext}:{onNext:()=>void}){const{state,saveQaAnswer}=useClub();const[index,setIndex]=useState(0);const[value,setValue]=useState(state.qaAnswers[`basic:${index}`]??'');const question=qaSets.basic[index];function save(){saveQaAnswer(`basic:${index}`,value);if(index<qaSets.basic.length-1){const next=index+1;setIndex(next);setValue(state.qaAnswers[`basic:${next}`]??'')}else onNext()}return <section className="onboarding-body"><div className="onboarding-intro"><span>QA 问答 · 初级必答</span><h1>慢一点，说一句真实的话</h1><p>自我探知、生活共识、情感理念。中高级题以后可以继续。</p></div><div className="qa-levels"><button className="is-active">初级 · 3题</button><button>中级 · 6题</button><button>高级 · 稍后</button></div><article className="qa-focus"><small>{index+1} / {qaSets.basic.length}</small><h2>{question}</h2><textarea aria-label="当前回答" value={value} onChange={event=>setValue(event.target.value)} placeholder="写下一句，或用语音说一段……"/><button className="voice-action" onClick={()=>setValue(value||'我更喜欢自然、不用刻意表现自己的时刻。')}><Mic size={18}/>语音说一段</button></article><div className="onboarding-actions"><button className="secondary-button" onClick={onNext}><SkipForward size={16}/>跳到基础资料</button><button className="primary-button" disabled={!value.trim()} onClick={save}>保存并继续</button></div></section>}
