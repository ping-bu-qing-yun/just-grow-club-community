import { Bookmark,Heart,MessageCircle } from 'lucide-react';import type { Need } from '../club/types';import { useClub } from '../club/ClubContext';
export function NeedCard({need,onOpen}:{need:Need;onOpen:(need:Need)=>void}){const{state,toggleNeedSaved,toggleNeedResonance}=useClub();const saved=state.savedNeedIds.includes(need.id),resonated=state.resonatedNeedIds.includes(need.id);return <article className="need-card-large"><button className="need-card-media" onClick={()=>onOpen(need)}><img src={need.image} alt=""/><span>{need.subtitle}</span></button><div className="need-card-content"><small>{need.author}</small><button className="need-card-title" onClick={()=>onOpen(need)}><h3>{need.title}</h3></button><p>{need.copy}</p><div className="club-tags">{need.tags.map(tag=><span key={tag}>{tag}</span>)}</div><div className="need-card-actions"><button className={resonated?'is-active':''} onClick={()=>toggleNeedResonance(need.id)}><Heart size={17}/>{need.resonance+(resonated?1:0)}</button><button onClick={()=>onOpen(need)}><MessageCircle size={17}/>{need.comments}</button><button className={saved?'is-active':''} onClick={()=>toggleNeedSaved(need.id)}><Bookmark size={17}/>{saved?'已收藏':'收藏'}</button></div></div></article>}
