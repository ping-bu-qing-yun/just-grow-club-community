# AGENTS.md

## 项目是什么

恰好俱乐部（Qiahao Club）微信小程序：帮小CC把用户的交友需求整理成画像，再反向生成线下活动、按需招募合适人选的 AI Agent Demo。目前是黑客松作品，核心交互链路已完成，正在走向真实可上线的小程序。

## 怎么运行和验证

- 用微信开发者工具导入本目录（`project.config.json` 在根目录）。
- AppID：`wxbbac26f6c800f503`（已配置在 `project.config.json`，可用于真机预览；正式发布前确认账号状态和主体资质）。
- 纯前端项目，无构建步骤；改完代码后在开发者工具模拟器里验证，涉及交互的改动必须用真机预览（iPhone + 安卓各一台）。
- 完整体验路径：欢迎页 → 三个基础问题 → 初级三张 QA 卡 → 选择中级话题 → 基础资料和头像 → 初回画像 → 活动报名。

## 目录结构

- `app.js` / `app.json` / `app.wxss`：小程序全局入口和配置。
- `pages/index/`：唯一页面，四个文件（wxml / wxss / js / json）。
- `assets/`：页面用到的图片、样式和脚本资源。
- `dist/`：网页版静态构建产物，一般不需要手动改。
- `小程序实现注意事项.md`：重要的实现约定和设备差异，动手前先读。

## 约定

- 布局用 `rpx`，设计基准 375x812，兼容小屏 iPhone 和常见安卓机。
- 主要按钮可点击高度不小于 80rpx；文本允许换行，不能写死宽度。
- 语音、头像、相册等真机相关功能必须真机测试，不能只看模拟器。
- 隐私相关：年龄、性别、语音等个人信息要谨慎处理，正式版要有隐私说明。

## 不要做

- 不要把 `.env`、`node_modules/`、`data/`、`.server-dist/`、提交用 ZIP 包提交到 Git。
- 不要把 AppSecret、数据库密码、真实用户信息放进仓库。
- 不要修改 `dist/` 构建产物而不改对应源码。
- 不要把网页版文件（`assets/`、`dist/`、`index.html`、提交包等）加回小程序打包：`project.config.json` 的 `packOptions.ignore` 已把它们排除，否则包会超过微信 2MB 上限，真机预览和上传都会失败。

## 完成标准

- 改动在开发者工具模拟器跑通，涉及交互的改动在真机预览通过。
- 相关注意事项同步更新到 `小程序实现注意事项.md`。
- 不引入未说明的新依赖，不破坏现有功能链路。
