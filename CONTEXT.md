# 恰好俱乐部（Qiahao Club）

一个面向上海 25–35 岁、想认真认识人但排斥强相亲与高消耗社交的人群的线下活动搭子产品。核心是「先理解用户为什么愿意出门，再在恰好场景里遇见恰好的人」。

## Language

### 角色与发布权

**管理者 / 小CC（Operator）**：
在产品里唯一对「活动」拥有发布权的角色；对应 PRD §4.3 的「小CC」。代码取值 `role = 'operator'`。可发布活动（走预活动→正式发布漏斗），亦可发布需求与生活（三项全能）。
_Avoid_：管理员、admin、超级管理员（除非未来明确要引入与 PRD 三角色并列的第四类）

**普通用户（Member）**：
可发布「需求」与「生活」，可报名、回应、收藏；不能发布「活动」。代码取值 `role = 'member'`。
_Avoid_：用户、user（在上下文里「用户」常泛指，需区分时用「普通用户」）

**主理人（Host）**：
从需求卡发起「活动提案」提交给小CC审核的角色（PRD §4.2、§9）；代码取值 `role = 'host'`。不直接发布正式活动——直接发布权仍只属于 operator。本期「+」菜单同普通用户（需求/生活）；提案闭环下期做。
_Avoid_：发起人、organizer

### 内容类型

**活动（Activity）**：
有主题、时间、地点、人数、费用、状态的活动单元；对应后端 `activities` 表，可报名、成群聊。
_Avoid_：局、event

**预活动（Pre-activity）**：
管理者填完活动表单后的首个状态；对用户可见作预告，但尚未开放报名。需管理者「正式发布」后才转为正式活动。
_Avoid_：草稿、draft（语义不同：预活动是已对用户可见的预告态，不是私有草稿）

**正式活动（Formal activity）**：
预活动经管理者「正式发布」后的状态；开放报名、可成局。对应 PRD §10.5 生命周期中「正式报名」起点。
_Avoid_：已发布活动（与"发布"动作一词混淆）

**需求（Need / NeedCard）**：
用户表达的关系/生活方式诉求卡片，发布后立即展示、不需小CC审核（PRD §3.1、§8.2）。当前由 `CreateNeedPage` 写入 `POST /api/v2/content`（`type=need`），正式状态以 `content_items + needs` 为唯一来源。
_Avoid_：需求卡（口语可，文档统称「需求」）、帖子、post

**生活（LifePost）**：
用户分享的日常/关系话题动态，以文字+图片为主；与「需求」并列、独立成页发布。当前入口由 `AppRouter` 路由到 `CreateLifePage`，后端通过 `POST /api/v2/content`（`type=life`）和 `content_items + life_posts` 完成发布闭环。
_Avoid_：动态（口语可，文档统称「生活」）、moment、朋友圈

## Flagged ambiguities

- **「需求」与「生活」的广场共处**：✅ 已定——保持现状结构：「需求」tab 内顶部 `需求 | 生活` 子 tab 切换，两段各自独立流（非混排）。发布入口已收敛为底部中央「+」（见 ADR-0002）；页面内发布按钮删除。
- **主理人是否在本需求范围内**：✅ 已定——按 PRD 三角色纳入，`role` 含 `'host'`（见 ADR-0003）。
- **活动发布的审核漏斗**：✅ 已定（见 ADR-0001）——管理者发布活动走「预活动→正式发布」两步；PRD §10.3–10.4 的 AI 提案/审核为独立的后续 P0 功能，本期不纳入。
- **「+ 按钮」的物理位置**：✅ 已定（见 ADR-0002）——底部导航正中央恢复凸起「+」作为全局发布入口；点击后弹出按角色分化的类型选择面板。这取代了 `refactor-bottom-nav-tabs`「删除发布按钮」的方向（四标签结构保留，「删 +」否决）。
- **页面内发布入口**：✅ 已定——全删。活动首页「发布活动」、需求页 hero「发布需求」均移除，只留底部中央「+」作为唯一发布入口。
- **活动首页快捷按钮**：✅ 已定——移除「发现更多活动」与「发布活动」两个按钮；保留画像条、精选、给你的见面、需求推荐等其余模块，调整后保持视觉平衡。
- **角色模型**：✅ 已定（见 ADR-0003）——`users.role` 取值 `'member' | 'host' | 'operator'`；演示账号 `13800000000`=operator，`13800000001`=member，`13800000002`=host。
- **管理者能否发需求/生活**：✅ 已定——三项全能。operator 的「+」菜单显示「活动 / 需求 / 生活」；member 仅显示「需求 / 生活」。
- **主理人「+」菜单与提案闭环本期范围**：✅ 已定——三角色枚举就位（`member|host|operator`）；host 的「+」菜单同 member（需求/生活）；「活动提案」闭环本期不做，需求详情不出现「我来发起活动」。
- **审核状态管理范围**：✅ 已定——活动有 `pre|formal|archived` 生命周期（ADR-0001）；需求与生活发布即上线，但仍可由运营治理为 rejected/archived（PRD §3.1）。
- **内容分类标签**：✅ 已定——活动保留 `category`，三类内容同时通过 `content_tags + content_item_tags` 使用统一标签字典；复合外键保证标签类型与内容类型一致。

## Example dialogue

> **Dev**：管理者点了发布，是要直接发活动，还是先走 AI 提案审核？
> **Domain expert**：先按直接发做——小CC本身就是审核关口，不要再给自己套一层审核漏斗。AI 提案是后面 P0 的事，现在 MVP 不掺进来。
> **Dev**：那普通用户呢？
> **Domain expert**：普通用户点同一个入口，只能选「需求」或「生活」，没有「活动」这一项。
